import 'package:flutter/material.dart';

/// Placeholder theme — flesh out with app_colors.dart / brand palette
/// whenever that's actually decided. Not blocking anything in Phase 1.
final ThemeData appTheme = ThemeData(
  useMaterial3: true,
  colorSchemeSeed: Colors.teal,
);